<h1> These be the tasks of the day</h1>

<ol>
  <% @tasks.each do |task| %>
  <li>
     <%= link_to task.title, task_path(task)%>
  </li>
  <% end %>
</ol>

<%= link_to "Create a new task", new_task_path()%>
